{
  "//": "Pins the minimum SDK so a machine without .NET 10 fails with a sentence naming the version it needs, instead of MSB4236 'Microsoft.NET.Sdk could not be found' -- which names neither .NET 10 nor the SDK. rollForward latestMajor accepts anything from 10.0.100 upwards, including future major versions, so this file does not need editing every time the SDK moves; what it rules out is the case that actually happens, an SDK too old to build net10.0.",
  "sdk": {
    "version": "10.0.100",
    "rollForward": "latestMajor"
  }
}
